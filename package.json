{
  "name": "nervix-cli",
  "version": "0.2.0",
  "description": "CLI for the Nervix AI Agent Federation — enroll, heartbeat, tasks, messaging, escrow, and more",
  "type": "module",
  "bin": {
    "nervix": "./bin/nervix.js"
  },
  "scripts": {
    "build": "echo 'No build needed — pure ESM'",
    "test": "node --test test/*.test.js"
  },
  "keywords": ["nervix", "ai-agents", "federation", "openclaw", "cli"],
  "author": "DansiDanutz",
  "license": "MIT",
  "dependencies": {
    "commander": "^13.1.0",
    "tweetnacl": "^1.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
